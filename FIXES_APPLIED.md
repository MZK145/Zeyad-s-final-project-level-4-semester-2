# Fixes applied

This branch is based on the current `main` branch.

- Hardened JWT configuration so the backend does not silently use the placeholder secret.
- Added explicit authentication input validation.
- Removed fallback default admin credentials from authentication logic.

